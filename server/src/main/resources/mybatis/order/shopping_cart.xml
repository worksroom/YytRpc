<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ShoppingCart">
    <resultMap type="ShoppingCart" id="ShoppingCartMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="goodsId" column="goods_id"/>
        <result property="shopId" column="shop_id"/>
        <result property="productId" column="product_id"/>
        <result property="sellUserId" column="sell_user_id"/>
        <result property="property" column="property"/>
        <result property="price" column="price"/>
        <result property="salePrice" column="sale_price"/>
        <result property="img" column="img"/>
        <result property="productName" column="product_name"/>
        <result property="createTime" column="create_time"/>
        <result property="buyNum" column="buy_num"/>
    </resultMap>
    <insert id="insert" parameterType="ShoppingCart">
        insert into shopping_cart (
        user_id, goods_id, shop_id, product_id, sell_user_id, property, price, sale_price, img, product_name, create_time, buy_num)
        VALUES
        ( #{userId}, #{goodsId}, #{shopId}, #{productId}, #{sellUserId}, #{property}, #{price}, #{salePrice}, #{img}, #{productName}, #{createTime}, #{buyNum})
        <selectKey resultType="int" keyProperty="id">
            select LAST_INSERT_ID() AS id
        </selectKey>
    </insert>
    <update id="update" parameterType="ShoppingCart">
        update shopping_cart set user_id = #{userId},goods_id = #{goodsId},shop_id = #{shopId},product_id = #{productId},sell_user_id = #{sellUserId},property = #{property},price = #{price},sale_price = #{salePrice},img = #{img},product_name = #{productName},create_time = #{createTime},buy_num = #{buyNum} where id=#{id}
    </update>
    <select id="select" parameterType="int" resultMap="ShoppingCartMap">
        select id ,  user_id, goods_id, shop_id, product_id, sell_user_id, property, price, sale_price, img, product_name, create_time, buy_num from shopping_cart where id=#{id}
    </select>
    <delete id="delete" parameterType="int">
        delete from shopping_cart where id=#{id}
    </delete>


    <select id="selectByUid" parameterType="int" resultMap="ShoppingCartMap">
        select id ,  user_id, goods_id, shop_id, product_id,
        sell_user_id, property, price, sale_price, img, product_name,
        create_time, buy_num from shopping_cart where user_id=#{userId} order by shop_id
    </select>

</mapper>