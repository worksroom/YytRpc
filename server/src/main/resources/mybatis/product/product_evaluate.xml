<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ProductEvaluate">
    <resultMap type="ProductEvaluate" id="ProductEvaluateMap">
        <result property="id" column="id"/>
        <result property="productId" column="product_id"/>
        <result property="totalNum" column="total_num"/>
        <result property="rate" column="rate"/>
        <result property="updateTime" column="update_time"/>
        <result property="goodsId" column="goods_id"/>
        <result property="productSpec" column="product_spec"/>
    </resultMap>
    <insert id="insert" parameterType="ProductEvaluate">
        insert into product_evaluate (
        product_id, total_num, rate, update_time, goods_id, product_spec)
        VALUES
        ( #{productId}, #{totalNum}, #{rate}, #{updateTime}, #{goodsId}, #{productSpec})
        <selectKey resultType="int" keyProperty="id">
            select LAST_INSERT_ID() AS id
        </selectKey>
    </insert>
    <update id="update" parameterType="ProductEvaluate">
        update product_evaluate set product_id = #{productId},total_num = #{totalNum},rate = #{rate},update_time = #{updateTime},goods_id = #{goodsId},product_spec = #{productSpec} where id=#{id}
    </update>
    <select id="select" parameterType="int" resultMap="ProductEvaluateMap">
        select id ,  product_id, total_num, rate, update_time, goods_id, product_spec from product_evaluate where id=#{id}
    </select>
    <delete id="delete" parameterType="int">
        delete from product_evaluate where id=#{id}
    </delete>
</mapper>